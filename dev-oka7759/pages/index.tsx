import type { NextPage } from 'next';
import styled from 'styled-components';

const Home: NextPage = () => {
  return <HomeBlock>s222</HomeBlock>;
};

export default Home;
const HomeBlock = styled.div`
  font-size: 55px;
`;
